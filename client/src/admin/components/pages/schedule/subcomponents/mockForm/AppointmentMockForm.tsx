import {AppointmentForm} from "@devexpress/dx-react-scheduler-material-ui";
import * as React from "react";

export default class AppointmentMockForm extends React.Component<AppointmentForm.OverlayProps> {
    render() {
        return (<></>)
    }
}